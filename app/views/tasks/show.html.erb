<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strong>
  <%= @task.description %>
</p>

<p>
  <strong>Completed:</strong>
  <%= @task.complete %>
</p>

<p>
  <strong>Due Date:</strong>
  <%= @task.due_date %>
</p>

<%= link_to 'Edit', edit_project_task_path(params[:project_id], @task) %> |
<%= link_to 'Back', project_tasks_path(@project) %>
